## SQLI-Query-Notes
### Authentication Bypass
```
● ' OR 1=1-- : closes the current string ('), adds an always-true condition (OR 1=1), and comments out the remaining query (--) to test for SQL injection vulnerabilities.
```











